import { BottomSheet } from '../Component';
import { type IBottomSheetProps } from '../types';

/** Story wrapper for react-docgen-typescript. */
export const BottomSheetStoryComponent = (props: IBottomSheetProps) => <BottomSheet {...props} />;

BottomSheetStoryComponent.displayName = 'BottomSheet';
