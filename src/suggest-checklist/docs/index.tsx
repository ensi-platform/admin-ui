import { SuggestChecklist } from '../Component';
import { type ISuggestChecklistProps } from '../types';

/** Story wrapper for react-docgen-typescript. */
export const SuggestChecklistStoryComponent = (props: ISuggestChecklistProps) => <SuggestChecklist {...props} />;

SuggestChecklistStoryComponent.displayName = 'SuggestChecklist';
