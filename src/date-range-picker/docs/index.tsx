import { DateRangePicker } from '../Component';
import { type IDateRangePickerProps } from '../types';

/** Story wrapper for react-docgen-typescript. */
export const DateRangePickerStoryComponent = (props: IDateRangePickerProps) => <DateRangePicker {...props} />;

DateRangePickerStoryComponent.displayName = 'DateRangePicker';
