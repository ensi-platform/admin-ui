/** Filters CSS variables from semantic tokens (--aui-filters-*). */
export const docsCssVariables = `/* CSS variables — Filters (--aui-filters-*) */
--aui-filters-gap-md: var(--aui-spacing-16); /* gap between cells and footer actions */
--aui-filters-pad-y-md: var(--aui-spacing-16); /* space above the footer */
--aui-filters-columns: 4; /* default equal tracks; Grid overrides per instance */
--aui-filters-span: 1; /* default cell span; Cell overrides per instance */
`;
