/** Default L0 width in px. */
export const DEFAULT_WIDTH = 280;

/** Default min L0 width while resizing. */
export const DEFAULT_MIN_WIDTH = 200;

/** Default max L0 width while resizing. */
export const DEFAULT_MAX_WIDTH = 400;

/** Collapsed icon-rail width in px. */
export const COLLAPSED_WIDTH = 64;

/** Flyout column width in px. */
export const FLYOUT_WIDTH = 280;

/** Synthetic L0 folder that opens the pinned list flyout. */
export const PINNED_ROOT_CODE = '__aui_pinned__';

/** Sync with `--aui-cascade-menu-duration-normal` / `--aui-duration-normal`. */
export const WIDTH_TRANSITION_MS = 200;
