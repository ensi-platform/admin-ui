/** Visible month window size for vertical scroll calendar. */
export const CALENDAR_WINDOW_MONTHS = 3;

/** Fallback year bounds when DatePicker has no min/max. */
export const DEFAULT_YEAR_MIN = 1900;
export const DEFAULT_YEAR_MAX = 2100;

/** Animated `scrollToMonth` duration. */
export const SCROLL_TO_MONTH_MS = 200;

/** Idle delay before week-row snap. */
export const WEEK_SNAP_IDLE_MS = 300;

/** Week-row snap animation duration. */
export const WEEK_SNAP_DURATION_MS = 150;
