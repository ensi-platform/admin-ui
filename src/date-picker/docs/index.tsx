import { DatePicker } from '../Component';
import { type IDatePickerProps } from '../types';

/** Story wrapper for react-docgen-typescript. */
export const DatePickerStoryComponent = (props: IDatePickerProps) => <DatePicker {...props} />;

DatePickerStoryComponent.displayName = 'DatePicker';
