/** Max simultaneously visible toasts when `maxVisibleToasts` is omitted. */
export const DEFAULT_MAX_VISIBLE_TOASTS = 5;

/** Auto-dismiss delay in ms when Provider `defaultTimeout` / per-call `timeout` omitted. */
export const DEFAULT_TIMEOUT = 5000;
