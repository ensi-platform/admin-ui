/** Page substrate CSS variables used by AdminUiProvider root (--aui-page-*). */
export const docsCssVariables = `/* CSS variables — AdminUiProvider page substrate (--aui-page-*) */
--aui-page-bg-primary: var(--aui-white); /* page canvas background */
--aui-page-fg-primary: var(--aui-black-900); /* page default foreground */
`;
