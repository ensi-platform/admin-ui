import { Tabs } from '../Component';
import { type ITabsProps } from '../types';

/** Story wrapper for react-docgen-typescript. */
export const TabsStoryComponent = (props: ITabsProps) => <Tabs {...props} />;

TabsStoryComponent.displayName = 'Tabs';
