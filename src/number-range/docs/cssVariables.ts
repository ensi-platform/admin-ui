/** NumberRange CSS variables from semantic tokens (--aui-number-range-*). */
export const docsCssVariables = `/* CSS variables — NumberRange (--aui-number-range-*) */
--aui-number-range-gap-md: var(--aui-spacing-8); /* gap between the from and to fields */
`;
