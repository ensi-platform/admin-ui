import { ComboboxListStatus, type IComboboxListStatusProps } from '../components/ListStatus';

/** Story wrapper for react-docgen-typescript. */
export const ComboboxStoryComponent = (props: IComboboxListStatusProps) => <ComboboxListStatus {...props} />;

ComboboxStoryComponent.displayName = 'Combobox';
