import { MultiAutocomplete } from '../Component';
import { type IMultiAutocompleteProps } from '../types';

/** Story wrapper for react-docgen-typescript. */
export const MultiAutocompleteStoryComponent = (props: IMultiAutocompleteProps) => <MultiAutocomplete {...props} />;

MultiAutocompleteStoryComponent.displayName = 'MultiAutocomplete';
