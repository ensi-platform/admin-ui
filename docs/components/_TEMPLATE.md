# <Name>

> Статус: `planned` | `wip` | `partial` | `ready`  
> База: `custom` | `lib: …` | `TBD`  
> Пакет: `admin-ui` | `admin-ui-layout` | `admin-ui-kit`

## Зачем

1–2 предложения: какую задачу закрывает в АП.

## Хотим видеть

Минимальный контракт до выбора либы. Чеклист, не полный API.

### Поведение

- [ ] …
- [ ] a11y: keyboard / focus / aria …

### API (черновик)

- `size`: …
- `variant`: …
- `dataTestId`
- `as` — да/нет
- …

### Состояния / визуал

- default / hover / focus / disabled / error / …
- light + dark

### Не в scope (v1)

- …

## Сравнение библиотек

| Кандидат | Плюсы | Минусы | Fit |
| --- | --- | --- | --- |
| `custom` | … | … | ? |
| `lib: …` | … | … | ? |
| `lib: …` | … | … | ? |

**Fit:** `good` / `ok` / `poor` — насколько ложится на наш стек (CSS Modules, tokens, без UI-kit «из коробки»).

Критерии (одинаковые для всех):

1. Стили снаружи / unstyled headless
2. a11y из коробки
3. размер бандла / deps
4. API не ломает наш контракт (`size` / `variant` / `dataTestId`)
5. поддержка / зрелость

## Решение

| Поле | Значение |
| --- | --- |
| База | `TBD` → … |
| Почему | … |
| Дата | YYYY-MM-DD |

## Открытые вопросы

- …
